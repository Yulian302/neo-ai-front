type PrivateRoute = {
  element: any;
  path: any;
};

type userLogin = {
  username: string;
  password: string;
};

type User = {
  id: number;
  username: string;
  password: string;
};

type AuthUserDetails = {
  isAuthenticated: boolean;
};
